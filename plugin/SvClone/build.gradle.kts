version = "1.0.2"
description = "Cloning Discord servers. | Based on https://bettercloner.vercel.app"

aliucord {
    author("Rhyan57", 896604349311115304L)

    changelog.set(
        """
            # 1.0.2
            * No more crashes
            * translated into English

            # 1.0.1
            * Corrigido erro de Iterator
            * GUI mais arredondada
            * Botao expandir/minimizar logs
            * Notificacoes de progresso
            * Logs no Aliucord
            * Botao no perfil do servidor corrigido
            
            # 1.0.0
            * Launch
        """.trimIndent()
    )
}
