version = "1.0.0"
description = "Apply Discord's official Nitro app themes to Aliucord, edit your profile gradient, and export Themer themes"

aliucord {
    author("rhyan57", 896604349311115304L)
    changelog.set("""
        # 1.0.0:
        - Initial release
        - Apply all 28 Discord Nitro color themes to Aliucord via Themer
        - Theme preview before applying (live preview)
        - Edit profile gradient colors and sync to Discord API
        - Export current theme as Themer JSON
        - Access via Appearance settings > Theme row
    """.trimIndent())
}
